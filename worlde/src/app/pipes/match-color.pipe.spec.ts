import { MatchColorPipe } from './match-color.pipe';

describe('MatchColorPipe', () => {
  it('create an instance', () => {
    const pipe = new MatchColorPipe();
    expect(pipe).toBeTruthy();
  });
});
